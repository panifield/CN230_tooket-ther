# คู่มืออธิบายโค้ดและสถาปัตยกรรมระบบ (Comprehensive Code & Architecture Guide) 🧠

เอกสารฉบับนี้จะอธิบายการทำงานของระบบ **Tooket-ther** อย่างละเอียดที่สุด ครอบคลุม **ทุกไฟล์ ทุกฟังก์ชัน** รวมถึงอธิบายว่าแต่ละส่วนเชื่อมโยงและทำงานร่วมกันอย่างไร เหมาะสำหรับผู้ที่ต้องการเข้าใจระบบแบบทะลุปรุโปร่ง

---

## 🌐 ภาพรวมโครงสร้างและการเชื่อมโยง (System Architecture & Data Flow)

ระบบของเราใช้โครงสร้างย่อยแบบ **Modular Routing** (แบ่งเป็นหมวดหมู่ตามหน้าที่) โดยมีศูนย์กลางอยู่ที่ `app.py`

1. **จุดศูนย์กลาง (`app.py`):** เปรียบเสมือนตึกอำนวยการ ที่คอยรับคำขอจากผู้ใช้ แล้วกระจายงานไปยังแผนกต่างๆ (ที่อยู่ในโฟลเดอร์ `routes/`)
2. **การตั้งค่าและฐานข้อมูล (`config.py` & `models.py`):** เป็นเหมือนสมุดกฎหมายและห้องเอกสาร ที่ทุกแผนกต้องอ้างอิงและใช้งานร่วมกัน
3. **แผนกต่างๆ (`routes/`):**
   * **`auth.py` (ฝ่ายทะเบียน):** จัดการเรื่องสมัครสมาชิก เข้าสู่ระบบ และยืนยันตัวตน
   * **`booking.py` (ฝ่ายขายตั๋ว):** คุมระบบคิว เลือกที่นั่ง และล็อคที่นั่ง
   * **`payment.py` (ฝ่ายการเงิน):** สร้าง QR Code และรับแจ้งผลการจ่ายเงินจากธนาคาร
   * **`refund.py` (ฝ่ายบริการลูกค้า):** รับเรื่องฟอร์มขอคืนเงิน
   * **`organizer.py` (ฝ่ายจัดการผู้จัดงาน):** ให้ผู้จัดงานสร้างคอนเสิร์ต คุมคิว และดูบัญชีรายรับ-รายจ่าย
4. **จุดเชื่อมโยงสำคัญ (`deps.py`):** เป็น "รปภ. ส่วนกลาง" ที่คอยตรวจบัตรประจำตัว (JWT Token) ก่อนให้ผู้ใช้เข้าถึงแผนกต่างๆ

---

## 📂 เจาะลึกทุกไฟล์และทุกฟังก์ชัน (File & Function Breakdown)

### 1. ไฟล์รากฐาน (Root Files)

#### 📝 `config.py` (ตั้งค่าระบบ)
*   **คลาส `Config`:** ทำหน้าที่ดึงค่าตัวแปรสภาพแวดล้อม (Environment Variables) ซ่อนความลับของระบบ เช่น รหัสผ่านฐานข้อมูล (DATABASE_URL), กุญแจลับสำหรับเข้ารหัส (JWT Secret), และข้อมูลเชื่อมต่อ Line/Google OAuth ข้อมูลนี้จะถูกเรียกใช้โดยไฟล์อื่นๆ ทั่วระบบเพื่อความปลอดภัย

#### 📝 `models.py` (จัดการการเชื่อมต่อฐานข้อมูล)
*   **`init_db_pool()` / `close_db_pool()`:** สร้างและปิด "สระน้ำ" ของ Connection ฐานข้อมูล (Connection Pool) เพื่อให้ระบบรองรับคนเข้าเว็บพร้อมกันจำนวนมากได้ โดยไม่ต้องเปิด-ปิดการเชื่อมต่อใหม่ทุกครั้ง
*   **`get_db_connection()`:** เป็นตัวแจกจ่าย Connection ให้กับฟังก์ชันอื่นๆ ใน `routes/` ที่ต้องการคุยกับฐานข้อมูล
*   **`fix_sequence()` / `fix_all_sequences()`:** ฟังก์ชันซ่อมแซมลำดับ ID ของตารางในฐานข้อมูล (มักใช้แก้ปัญหาเวลาเราแทรกข้อมูลทดสอบเข้าไปแล้วเลข ID รันต่อไม่ถูกต้อง)

#### 📝 `app.py` (จุดเริ่มต้นของระบบ)
*   **`_expire_bookings_loop()`:** "พนักงานทำความสะอาดเบื้องหลัง" ที่รันวนลูป (`while True`) ทุกๆ 60 วินาที คอยเช็คในตาราง `booking` ว่าการจองไหนหมดเวลา 15 นาทีแล้วยังไม่จ่ายเงิน ระบบจะอัปเดตเป็น 'expired', คืนที่นั่ง (Seat) เป็น 'available', และลบคิวให้โดยอัตโนมัติ
*   **`lifespan()`:** ฟังก์ชันที่คุมวงจรชีวิต ทำงานตอน "เปิดเซิร์ฟเวอร์" (สร้าง DB Pool และสตาร์ทพนักงานทำความสะอาด) และตอน "ปิดเซิร์ฟเวอร์" (ปิดฐานข้อมูล)
*   **`root()` / `health_check()`:** ฟังก์ชันพื้นฐานเช็คว่าระบบ API และฐานข้อมูลยังทำงานเชื่อมต่อกันปกติดีหรือไม่
*   **`app.include_router(...)`:** จุดเชื่อมต่อที่นำแผนกต่างๆ จากโฟลเดอร์ `routes/` มาประกอบเข้ากับตึกอำนวยการหลัก

---

### 2. โฟลเดอร์ `routes/` (แผนกบริการต่างๆ)

#### 🛡️ `deps.py` (ระบบยืนยันตัวตนกลาง)
*   **`get_current_user()`:** รับกุญแจ JWT ที่ผู้ใช้ส่งมา ถอดรหัส ตรวจสอบว่าหมดอายุหรือยัง แล้ววิ่งไปค้นหาในฐานข้อมูลว่า User คนนี้คือใคร มีสิทธิ์ (Role) อะไร จากนั้นส่งข้อมูลนี้กลับไปให้หน้าเส้นทางอื่นๆ (Route) ใช้ต่อ ระบบนี้ถูกใช้แทบทุกที่เพื่อป้องกันคนนอก

#### 👤 `auth.py` (ระบบสมาชิกและความปลอดภัย)
*   **`_hash_password()` / `_issue_jwt()` (ฟังก์ชันย่อย):** แปลงรหัสผ่านเป็นรหัสลับแบบ SHA-256 และทำหน้าที่ออกกุญแจดิจิทัล (JWT Token) ให้ผู้ใช้พกติดตัว
*   **`register()`:** รับข้อมูลผู้ใช้ใหม่ ตรวจสอบอีเมลซ้ำ บันทึกลงตาราง `users` และตรวจสอบสิทธิ์ หากเป็นลูกค้าจะสร้าง `customer_profile` ถ้าระบุว่าเป็นผู้จัดก็จะสร้าง `organizer_profile` เตรียมไว้ให้
*   **`login()`:** ตรวจสอบอีเมลและรหัสผ่าน หากถูกต้องจะเรียก `_issue_jwt()` เพื่อออกกุญแจให้
*   **`oauth_authorize_url()` / `oauth_callback()`:** ระบบ Login ทันสมัยผ่าน Line และ Google โดยสร้าง URL ให้ผู้ใช้คลิกไปล็อกอิน และรับรหัสกลับมาเพื่อสร้างบัญชีหรือล็อกอินให้ทันทีโดยอัตโนมัติ
*   **`get_me()` / `update_profile()` / `forgot_password()`:** ให้ผู้ใช้ดูข้อมูลส่วนตัว, แก้ไขชื่อ-เบอร์โทร-ที่อยู่, และฟังก์ชันรีเซ็ตรหัสผ่านหากลืม

#### 🎟️ `booking.py` (ระบบจองตั๋วและคิวอัจฉริยะ)
*   **`queue_join()`:** ฟังก์ชันให้ลูกค้ากดเข้าคิว ระบบจะคำนวณ **Priority Score** (ถ้าที่อยู่ลูกค้าตรงกับจังหวัดที่จัดงาน จะได้คะแนน 100 เพื่อให้สิทธิ์คนในพื้นที่ก่อน) และบันทึกลงตาราง `queue_session`
*   **`queue_status()`:** คำนวณว่าตอนนี้ลูกค้าอยู่คิวที่เท่าไหร่ โดยสั่งฐานข้อมูลให้นับจำนวนคนที่มีคะแนนสูงกว่า หรือคนที่เข้าคิวก่อนเรา
*   **`get_concerts()` / `get_zones()` / `get_seats()`:** ฟังก์ชันดึงข้อมูลดึงรายการคอนเสิร์ต โซนที่จัด และผังที่นั่งทั้งหมด ส่งให้หน้าเว็บไปแสดงผลเป็นกราฟิก
*   **`book_seats()` (หัวใจหลัก):** เมื่อลูกค้าเลือกที่นั่งแล้วกดจอง ฟังก์ชันนี้จะใช้คำสั่ง `SELECT ... FOR UPDATE` เพื่อล็อคแถวข้อมูลที่นั่งนั้นไม่ให้ใครแตะต้อง เปลี่ยนสถานะที่นั่งเป็น 'locked', สร้าง `booking` (กำหนดเวลาจ่ายเงิน 15 นาที), และสร้างตั๋ว (`ticket`) พร้อมสุ่ม QR Code ลับให้ตั๋วแต่ละใบ ทุกอย่างทำผ่าน Transaction ถ้าพลาดขั้นตอนเดียวจะยกเลิกทั้งหมด
*   **`my_bookings()` / `confirm_booking()` / `rebook_zone_closure()`:** ฟังก์ชันดูประวัติการจองของตัวเอง, ฟังก์ชันจำลองการยืนยันการจ่ายเงิน (สำหรับรอบทดสอบ), และฟังก์ชันให้ลูกค้าย้ายที่นั่งฟรีๆ ในกรณีที่เกิดเหตุการณ์ผู้จัดงานสั่งปิดโซนกะทันหัน

#### 👔 `organizer.py` (ระบบสำหรับผู้จัดงาน)
*   **`get_concert_queues()` / `admit_customer_queue()`:** ผู้จัดงานสามารถกดเข้ามาดูรายชื่อคนรอคิวทั้งหมด และกด "Admit" เพื่อเปิดประตูอนุญาตให้คิวเข้าไปถึงหน้าเลือกที่นั่งได้
*   **`update_queue_priority()` / `auto_sort_queues()`:** ฟังก์ชันปรับเปลี่ยนคะแนนคิวด้วยมือผู้จัดเอง หรือสั่งให้ระบบคำนวณให้ใหม่ตามที่อยู่ของผู้เข้าคิวทั้งหมด
*   **`create_concert()`:** ผู้จัดกรอกข้อมูลจัดคอนเสิร์ต ฟังก์ชันจะรับข้อมูลรูปภาพ (บันทึกเซฟลงในเครื่องเซิร์ฟเวอร์), สร้างตารางคอนเสิร์ต, สร้างโซนราคาต่างๆ, และสร้างผังเก้าอี้ (Seat) ทีละตัวลงฐานข้อมูลอัตโนมัติตามจำนวนที่ระบุ (เช่น โซน A มี 100 ที่ ระบบก็สร้าง A1 ถึง A100 ให้เลย)
*   **`close_zone()`:** หากโซนไหนขายบัตรได้น้อย ผู้จัดกดปิดโซนผ่านฟังก์ชันนี้ ระบบจะเซ็ตให้โซนนั้นหายไป และเปลี่ยนสถานะลูกค้าที่จ่ายเงินซื้อโซนนั้นไปแล้วเป็น 'zone_closed_action_required' (คือได้สิทธิ์เป็น Voucher เพื่อเปลี่ยนโซนหรือขอเงินคืน) และมีการส่งอีเมลแจ้งเตือน
*   **`get_concert_dashboard()`:** สรุปหน้าแดชบอร์ด โดยดึงข้อมูลจากตารางการจ่ายเงิน (`payment`) หักลบกับการคืนเงิน (`refund`) นำมาประมวลผลจัดกลุ่มแยกตามวัน เพื่อสรุปเป็นกราฟรายได้-รายจ่าย

#### 💸 `payment.py` (ระบบรับชำระเงินและ Webhook)
*   **`build_promptpay_payload()` (ฟังก์ชันย่อย):** แปลงเบอร์โทรศัพท์/บัตรประชาชน และจำนวนเงิน ออกมาเป็นรหัสตัวเลขยาวๆ (Payload) เพื่อส่งให้หน้าเว็บไปแปลงเป็นรูป PromptPay QR Code
*   **`generate_qr()`:** รับคำขอจ่ายเงินจากหน้าเว็บ เช็คว่าเวลา 15 นาทียังไม่หมด จากนั้นบันทึกตาราง `payment` สถานะ 'pending' สร้างรหัสอ้างอิง `transaction_ref` และคืนค่า Payload ไปให้
*   **`payment_status()`:** เป็นจุดที่หน้าเว็บยิงคำถามมารัวๆ ทุก 3 วินาที (Polling) ว่า "จ่ายเงินหรือยัง? ธนาคารแจ้งมาหรือยัง?"
*   **`payment_webhook()` (จุดเชื่อมโลกภายนอก):** ฟังก์ชันนี้เปิดประตูรอรับสัญญาณจากธนาคาร (Gateway) เมื่อลูกค้าโอนเงินผ่านมือถือ ธนาคารจะยิงสัญญาณมาที่นี่ ระบบจะตรวจสอบลายเซ็นความปลอดภัย (HMAC Signature) ก่อน ถ้าของจริง จะทำการแก้ไข Payment เป็น 'paid', เปลี่ยนเก้าอี้เป็น 'sold', ตัดคิวให้จบกระบวนการ, และบันทึกลงบัญชีตาราง `finance`

#### ↩️ `refund.py` (ระบบขอเงินคืน)
*   **`request_refund()`:** ลูกค้าที่จ่ายเงินไปแล้ว เกิดเปลี่ยนใจกดยื่นขอคืนเงินด้วยตัวเอง (ภายในระยะเวลา 7 วัน) ฟังก์ชันจะเช็คเงื่อนไขเวลา, สร้างรายการในตาราง `refund`, เปลี่ยนที่นั่งลูกค้ากลับเป็น 'available' (เพื่อให้ระบบเอาตั๋วใบนั้นไปขายคนอื่นต่อได้), และบันทึกรายจ่ายลงบัญชี
*   **`refund_voucher()`:** กรณีพิเศษสำหรับคนที่โดนผู้จัด "ปิดโซน" (จากไฟล์ organizer.py) ลูกค้าสามารถเรียกใช้ฟังก์ชันนี้เพื่อขอรับเงินคืนเต็มจำนวนได้ทันที โดยไม่มีข้อจำกัดเรื่องเวลา 7 วัน

---

## 🔗 สรุปวงจรการเชื่อมโยง (The Full Workflow)

1.  **เข้าใช้งาน (Auth Flow):** เริ่มต้น Request วิ่งเข้า `app.py` -> ส่งต่อไป `auth.py` -> ตรวจสอบข้อมูลใน Database -> ออกกุญแจ Token
2.  **ตรวจบัตรผ่าน (Security Flow):** เมื่อลูกค้าจะเข้าคิว จะต้องแนบ Token มา -> ฟังก์ชัน `get_current_user()` ใน `deps.py` ทำการรับรองความถูกต้อง
3.  **เลือกรอบ & เข้าคิว (Queue Flow):** โทเค็นผ่าน -> วิ่งเข้า `booking.py` เรียกคำสั่ง `queue_join()` -> `organizer.py` เช็คความเรียบร้อยแล้วสั่งเปิดประตู `admit_customer_queue()`
4.  **จองที่นั่ง (Transaction Flow):** ลูกค้าเข้าหน้าเลือกที่นั่ง แล้วกดจองผ่าน `book_seats()` ใน `booking.py` -> Database ทำการล็อคแบบ FOR UPDATE -> นับเวลาถอยหลัง
5.  **จ่ายเงิน (Payment Flow):** ขอ QR จาก `payment.py` -> นำไปสแกนจ่าย -> ธนาคารยิงมายืนยันที่ `payment_webhook()` -> ยืนยันการจอง 
6.  **ลบข้อมูลขยะ (Expiry Flow):** หากขั้นตอนที่ 5 ทำไม่เสร็จภายใน 15 นาที พนักงานกะดึก `_expire_bookings_loop()` ใน `app.py` จะตามมาล้างข้อมูลและคืนที่นั่ง
7.  **รายงานผล (Dashboard Flow):** ผู้จัดงานสามารถตรวจดูรายได้รวมทั้งหมดผ่านฟังก์ชัน `get_concert_dashboard()` ใน `organizer.py` และหากงานมีปัญหา ลูกค้าก็ขอยกเลิกและรับเงินคืนผ่าน `refund.py`

ทุกฟังก์ชัน ทุกไฟล์ ถูกออกแบบมาให้ทำงานประสานกันเหมือนเฟืองในนาฬิกา โดยมี **Database เป็นแกนกลาง** และ **FastAPI เป็นตัวขับเคลื่อน** ครับ!

